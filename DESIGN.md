# 知乎·算命 Agent 设计文档

> 目标：用户输入信息 → 给出**有古籍依据、不模棱两可**的算命结果。
> 本文档回答两个问题：这件事是否可行？如果可行，系统应该怎么设计？

---

## 一、可行性评估

**结论：可行，但前提是把"科学的算命"重新定义为"可复现、可溯源的算命"。**

### 1.1 三个关键判断

**判断一："科学"不能指"预测准确"，只能指"过程严谨"。**

科学意味着可证伪、可重复。没有任何占卜方法能通过这个检验，宣称"科学算命"
既名不副实，也有广告法风险（见 §9 合规）。但有两个性质是可以真正做到的：

- **可复现**：同样的输入（问题 + 时间 / 随机种子）必然得到同一卦。起卦过程
  公开算法、公开种子，用户可以自己验证。
- **可溯源**：结果中的每一句论断都能逐字对应到《周易》等公版典籍的原文，
  附书名、篇章、原文引文，用户可以翻书核对。

这两点合起来，就是这个产品真正的差异化卖点——市面上的算命应用做不到，
也不敢做。宣传口径用"有典可依、可考据、可复现"，不用"科学"二字。

**判断二："不模棱两可"恰好是《周易》的长处，但要放对位置。**

传统算命"准"的心理机制是巴纳姆效应——话说得越模糊，越容易对号入座。
给出明确结论意味着放弃这个保护伞：明确 + 说错 = 用户不满。

但《周易》的文本结构恰好支持明确结论：卦辞爻辞自带**断辞**——吉、凶、
悔、吝、厉、无咎、亨、利——这是古人明文写下的判断，不是我们编的。
"初九，潜龙勿用"就是明确地说"不要行动"；"利涉大川"就是明确地说"宜进"。

因此明确性的正确落点是：

- ✅ 明确的**判断与建议**（此事：凶，不宜进；宜守静待时）——直接映射自断辞
- ❌ 明确的**事件预言**（"你下个月会升职"）——必错无疑，且涉合规风险

敢于如实输出"凶、不宜"，反而是最大的差异化：别家不敢说凶，我们说的是
《周易》原文说凶。权威感和责任都落在典籍上，不在产品上。

**判断三：用户满意度的来源是可解释性 + 仪式感，不是命中率。**

引用原文带来权威感；公开起卦过程带来参与感和信任感；明确结论带来
"得到了答案"的满足感。这三者都不依赖预测是否应验，因此产品逻辑成立。

### 1.2 主要风险

| 风险 | 说明 | 缓解 |
|------|------|------|
| 明确结论被证伪 | 用户记住错的预测 | 结论落在"建议"而非"预言"；忠于原文断辞，责任归典籍 |
| LLM 幻觉引文 | 编造不存在的"古籍原文" | 引文逐字校验闸门（§5.6），不匹配即拒绝输出 |
| 平台审核 | 微信小程序明确禁占卜类目；App Store 中国区审核严格 | 以"国学文化 / 传统典籍学习"定位上架，占断作为典籍应用场景呈现 |
| 内容红线 | 医疗、投资、重大决策的确定性断言 | 问题分类器前置拒答 + 免责声明（§9） |

---

## 二、核心设计原则

整个系统只有一条主线原则，其余都是它的推论：

> **LLM 不算卦，只解释。所有"算"的部分是确定性代码；所有"引"的部分逐字
> 校验；LLM 只负责把规则选定的原文讲清楚。**

四条推论：

1. **计算与解释分离**——起卦、变爻、选文全部是纯函数，单元测试全覆盖，
   同输入必同输出。这是"可复现"的来源。
2. **典籍是唯一事实来源**——LLM 的解读只能基于系统喂给它的原文片段，
   每个论断强制附出处。这是"可溯源"的来源。
3. **规则选文，杜绝挑拣**——读卦辞还是读哪条爻辞，由朱熹《易学启蒙》
   占法规则（§8 附录A）决定，写死在代码里。LLM 无法挑一句对用户好听的。
4. **结论来自断辞映射，不来自 LLM 发挥**——"吉/凶/宜/忌"的最终判定由
   断辞映射表（§8 附录B）确定性给出，LLM 不得改判。

---

## 三、系统架构

```
用户输入（问题 + 起卦方式 + 可选时间/数字）
      │
      ▼
┌─────────────────┐
│ ① 问事解析层     │  问题分类（事业/情感/出行/决策…）
│   （LLM，轻量）  │  红线类别拒答（医疗/投资/法律）
└────────┬────────┘
         ▼
┌─────────────────┐
│ ② 起卦引擎       │  纯函数。梅花易数时间起卦（完全确定性）
│   （确定性代码） │  或铜钱法（公开 seed，可复现）
└────────┬────────┘  输出：本卦、动爻、之卦、互卦
         ▼
┌─────────────────┐
│ ③ 断卦规则引擎   │  朱子占法：按变爻数确定读哪条经文（附录A）
│   （确定性代码） │  断辞 → 结论映射（附录B）
└────────┬────────┘  输出：选定经文ID列表 + 确定性结论
         ▼
┌─────────────────┐
│ ④ 典籍知识库     │  结构化查表（非向量检索，见 §5.4）
│   （SQLite/JSON）│  经文原文 + 象传彖传 + 历代注疏
└────────┬────────┘
         ▼
┌─────────────────┐
│ ⑤ 解读生成层     │  Claude API，结构化输出
│   （LLM）        │  只解释，不改判，逐句带出处
└────────┬────────┘
         ▼
┌─────────────────┐
│ ⑥ 引文校验器     │  每条引文逐字匹配知识库
│   （确定性代码） │  不匹配 → 拒绝输出，重新生成
└────────┬────────┘
         ▼
    结果呈现（卦象图 + 原文 + 白话 + 明确结论 + 出处 + 复现凭证）
```

---

## 四、各模块设计

### 4.1 输入层 / 问事解析

- 用户输入：所问之事（自由文本），可选生辰、可选自选数字。
- LLM 做两件事：
  1. **分类**：事业、学业、情感、人际、出行、居所、决策抉择、其他。
     分类结果用于解读时的语境适配（同一爻辞在问事业和问出行时侧重不同，
     这在《增删卜易》等断法典籍中本有分类论述）。
  2. **红线拦截**：医疗健康诊断、投资理财、法律纠纷、生死寿夭类问题
     直接婉拒并说明原因。这一层宁可错杀。

### 4.2 起卦引擎（确定性代码）

支持两种起卦方式，各有明确的可复现机制：

**方式一：梅花易数·时间起卦（默认，推荐）**

完全确定性，无随机数。规则出自《梅花易数》卷一：

- 上卦 =（农历年支数 + 月数 + 日数）mod 8
- 下卦 =（农历年支数 + 月数 + 日数 + 时辰数）mod 8
- 动爻 =（年 + 月 + 日 + 时）mod 6
- （余数为 0 者取 8 / 6）

同一时辰问同一事，任何人复算结果一致。需要一个公历→农历转换库，
这是本方式唯一的外部依赖，须锁定版本并对边界日期做测试。

**方式二：铜钱法·六爻（金钱卦）**

规则出自《卜筮正宗》：三枚铜钱掷六次，三背为老阳(9)、三字为老阴(6)、
一背二字为少阳(7)、二背一字为少阴(8)，老阳老阴为动爻。

随机性处理：`seed = SHA-256(问题文本 + 用户ID + 请求时间戳)`，seed 与
算法一并展示给用户。事后任何人拿同一 seed 可复现全部六次掷币结果。

注意：铜钱法四种爻的概率是 1/8、1/8、3/8、3/8，与大衍筮法的
1/16、3/16、5/16、7/16 分布不同。实现哪种就如实标注哪种——这类细节
上的诚实正是"严谨"人设的组成部分。

**输出数据结构：**

```json
{
  "method": "meihua_time | coin",
  "reproducibility": { "seed": "…", "lunar_date": "…", "algorithm": "…" },
  "ben_gua":  { "id": 1, "name": "乾", "lines": [7,7,7,9,7,7] },
  "moving_lines": [4],
  "zhi_gua":  { "id": 9, "name": "小畜" },
  "hu_gua":   { "id": 1, "name": "乾" }
}
```

### 4.3 断卦规则引擎（确定性代码）

两个职责，都是查表，都可单元测试：

1. **选文**：按变爻数量套用朱熹《易学启蒙》占法（附录A），输出应读的
   经文 ID（某卦卦辞 / 某爻爻辞）。结果页面标注"依朱熹《易学启蒙》占法"。
2. **定性**：从选定经文的断辞（吉/凶/悔/吝/厉/无咎/亨/利）经附录B的
   映射表得出结论字段，例如 `{"verdict": "凶", "action": "不宜进，宜守"}`。
   这个字段 LLM 只能解释，不能修改。

### 4.4 典籍知识库

**关键决策：用结构化查表，不用向量检索（RAG）。**

理由：核心语料是封闭且极小的——64 卦 ×（卦辞 + 6 爻辞 + 彖传 + 大小象传）
+ 系辞等，全部《周易》经传不足三万字。断卦规则引擎已经精确知道要读哪条，
按 ID 查表即可，检索准确率 100%，没有 embedding 的近似误差。这本身就是
"严谨"的一部分。

**Schema（每爻一条记录）：**

```json
{
  "id": "01-4",
  "hexagram": "乾", "position": "九四",
  "text": "或跃在渊，无咎。",
  "xiang": "或跃在渊，进无咎也。",
  "duanci": ["无咎"],
  "commentaries": [
    { "source": "周易本义", "author": "朱熹", "dynasty": "宋", "text": "…" },
    { "source": "周易注",   "author": "王弼", "dynasty": "魏", "text": "…" }
  ],
  "topic_notes": { "出行": "…", "事业": "…" }
}
```

**语料范围与版权：**

- 第一层（MVP 必须）：《周易》经文 + 《易传》十翼。先秦文本，无版权。
- 第二层：王弼《周易注》、程颐《伊川易传》、朱熹《周易本义》。古人著作
  本身是公版；注意不能直接抓取现代整理本/标点本（整理成果可能有邻接权），
  应基于公版影印文本自行录入校对，或使用明确开放授权的电子文本。
- 第三层（后续扩展）：《梅花易数》《增删卜易》《卜筮正宗》的分类断法，
  用于 `topic_notes` 字段。

数据制作是本项目**工作量最大的部分**（预计远超代码），需要人工校对流程。

### 4.5 解读生成层（LLM）

- 模型：Claude API（claude-sonnet-5 起步，效果不足再上 claude-opus-5）。
- 使用结构化输出（tool use / JSON schema）强制以下输出结构：

```json
{
  "hexagram_display": "卦象与卦名说明",
  "scripture": [ { "quote": "原文逐字引用", "source": "周易·乾·九四" } ],
  "translation": "白话直译",
  "interpretation": "针对用户所问之事的解读（每个论断句尾标注出处编号）",
  "verdict": "〔只读，来自规则引擎，LLM 原样复述〕",
  "advice": "基于断辞与爻义的具体行动建议",
  "citations": [ … ]
}
```

- System prompt 核心约束：
  1. 只能使用输入中给出的经文与注疏，不得引入任何外部"典籍"内容；
  2. `verdict` 字段来自规则引擎，只许解释不许改判、不许软化
     （不许把"凶"说成"略有不顺"）；
  3. 解读须紧扣用户所问类别，避免泛泛的人生哲理；
  4. 语气：如实、克制、不恐吓、不承诺。

### 4.6 引文校验器（幻觉闸门）

生成后、展示前的最后一道确定性检查：

- `scripture[].quote` 的每一条，逐字（去标点后）在知识库中精确匹配，
  且其 `source` 标注与匹配记录一致；
- `verdict` 与规则引擎输出逐字一致；
- 任一条不过 → 丢弃本次生成，带失败原因重试（上限 3 次），仍失败则
  降级为"仅展示原文 + 白话 + 确定性结论"的无解读模式。

这道闸门保证了产品的核心承诺——"每句话有出处"——不依赖 LLM 的自觉。

### 4.7 多轮对话（Agent 部分）

真正需要 agent 化的是解读之后的追问，而非算卦本身：

- "九四爻是什么意思？"——追问经文细节，仍走知识库 + 引文校验；
- "如果我换个时间问呢？"——解释起卦机制，引导重新起卦（并说明
  《易经》"初筮告，再三渎，渎则不告"的传统——同一事短期内重复占问，
  产品层面应有冷却期，这既合古义又防刷）；
- 追问不得产出新的 verdict，只能围绕已出结果解释。

会话状态：`{ 问题, 卦象, 选文, verdict, 对话历史 }`，用 Claude Agent SDK
或简单的会话循环实现即可，不需要复杂的多 agent 编排。

### 4.8 呈现层

- 起卦过程动画（蓍草/掷钱/时间取数），营造仪式感；
- 卦象图 + 动爻高亮；
- 结果页固定结构：原文（竖排样式可加分）→ 白话 → 解读 → **明确结论**
  （视觉上最突出）→ 建议 → 出处列表；
- **"验证此卦"按钮**：展示起卦输入、算法说明、seed，附复现步骤——
  这是"可复现"卖点的落地界面；
- 免责声明常驻页脚。

---

## 五、技术选型建议

| 部分 | 选型 | 理由 |
|------|------|------|
| 后端 | Python + FastAPI | 起卦引擎纯函数易测试；农历转换有成熟库 |
| 知识库 | SQLite（或直接 JSON 文件） | 语料 < 1MB，查表足矣 |
| LLM | Claude API，结构化输出 | 强制 citation 字段 |
| 前端 | 小程序 / H5（视上架策略） | 见 §9 合规 |
| 测试 | pytest，64 卦 × 7 种变爻情形全覆盖 | 确定性部分必须 100% 覆盖 |

---

## 六、MVP 范围与路线图

**MVP（建议 2–3 周内可验证）：**

1. 只做梅花易数时间起卦（零随机数，最能体现"可复现"）；
2. 知识库只含《周易》经文 + 大小象传（数据量最小的完整闭环）；
3. 完整跑通：问事 → 起卦 → 朱子占法选文 → 断辞结论 → LLM 解读 →
   引文校验 → 结果页（含验证按钮）；
4. 不做多轮追问、不做注疏层、不做账户体系。

**此后按序扩展：** 铜钱法 → 注疏层（朱熹/程颐/王弼）→ 分类断法
（topic_notes）→ 多轮追问 → 回访机制（"当时问的事后来如何"，既是留存
手段，也积累了独一无二的数据）。

**明确不做：** 八字、紫微斗数等其他术数（v1 聚焦易经，摊子铺大则
数据工程失控）；任何形式的付费改运、开光、法物销售（合规红线）。

---

## 七、合规与伦理（务必重视）

1. **平台审核**：微信小程序类目明确不含且实际禁止"占卜"；App Store
   中国区对迷信内容审核严格。上架定位应为**国学/传统文化教育工具**，
   产品叙事为"《周易》典籍的检索、学习与传统占法演示"。这不是话术
   包装——本设计的可溯源、标注占法出处、展示原文，实质上就是典籍
   应用工具。
2. **宣传用语**：不使用"科学算命""预测准确"等表述（广告法 + 名不副实）。
   使用"有典可依、可考据、可复现"。
3. **红线内容**：医疗、投资、法律、寿夭类问题一律拒答（§4.1）。
4. **免责声明**：所有结果页注明"内容源自古代典籍原文及传统占法，属
   传统文化范畴，仅供参考，不构成任何现实决策依据"。
5. **未成年人**：如平台要求，加年龄提示。

---

## 八、附录

### 附录A：朱子占法（《易学启蒙》）——变爻选文规则

| 变爻数 | 所读经文 |
|--------|----------|
| 0 | 本卦卦辞 |
| 1 | 本卦变爻爻辞 |
| 2 | 本卦两变爻爻辞，以上爻为主 |
| 3 | 本卦卦辞（贞）+ 之卦卦辞（悔）合参 |
| 4 | 之卦两不变爻爻辞，以下爻为主 |
| 5 | 之卦不变爻爻辞 |
| 6 | 乾坤两卦读用九/用六，余卦读之卦卦辞 |

（梅花易数起卦固定一个动爻，规则简化为：本卦卦辞为体，动爻爻辞为断，
之卦卦辞为势——依《梅花易数》体用之说。）

### 附录B：断辞 → 结论映射（示例，需随数据工程细化）

| 断辞 | verdict | action 基调 |
|------|---------|-------------|
| 元吉 / 大吉 | 吉 | 宜进，可大胆行动 |
| 吉 / 亨 / 利… | 吉 | 宜进 |
| 无咎 | 平 | 可行，行动本身无过失 |
| 悔 / 吝 | 谨 | 可行但有代价，宜慎 |
| 厉 | 危 | 有风险，非必要不动 |
| 凶 | 凶 | 不宜进，宜守静待时 |
| 无断辞 | 依象传定性 | 由象传文意映射，映射表穷举 384 爻逐条人工审定 |

注：一条爻辞可含多个断辞（如"贞吉，悔亡"），映射表须按整句人工审定，
不做自动拆词——这也是数据工程的一部分。

---

## 九、一句话总结

**把"算"做成可复现的确定性代码，把"据"做成逐字可校验的典籍查表，把
LLM 限制在"解释者"的位置上，把"明确结论"锚定在古人明文写下的断辞上——
这样"有依据、不模棱两可"的承诺就不依赖模型的自觉，而是系统结构保证的。**
