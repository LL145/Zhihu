# 天问 设计文档（v2：易经 + 星盘双引擎）

> 目标：用户输入信息 → 给出**有古籍依据、不模棱两可**的算命结果。
> v2 变更：新增星盘"命引擎"，与易经"事引擎"构成双引擎；两引擎共用
> 同一条"确定性计算 + 典籍查表 + LLM 只解释 + 引文校验"流水线。

## 定位修订（v3）：还原占法本来流程，LLM 任占者之判

古之占者一身兼四事：**依法起卦**（机械仪程）、**循例选文**（占法定例）、
**衡文而断**（活的裁量——《左传》穆姜、子服惠伯诸案所示，断可衡情，
甚至据德异于文面）、**为问者讲释**。本品照此还原分工：

- 起卦/排盘/选文/召回——确定性代码循典籍明文机行，与古法中本就机械的
  环节一致；
- **断与释——LLM 任之**：如古之占者衡规则选定的原文而下占断（judgment），
  句句须标出处（**无据不断**，校验器强制）；「定例断辞」（自经文断辞
  字样/庙陷明文机取，即 v2 的 verdict）降为**对照基准**——占断从之须明
  其所以然，异断须明所据之文与其理；
- 占德红线（「《易》不可以占险」）照旧机械拦截。

**承诺口径相应修订**：本品不保证灵验；保证**正统**（流程与文本俱出典籍
明文，引文逐字校验）与**有条件的可复现**——确定性各层由种子与时刻唯一
确定、可重放复算；占断出自模型（同问未必同断，如古之占者），每次输出附
**占断存证**（模型名 + 输出 JSON 的 SHA-256）：定种子与存证，即定全局。
本文其余章节中"可复现"的绝对表述，一律依此读作"有条件可复现"；
"LLM 只解释、不得改判"的表述由本节取代（引擎间禁折中之 §6.3 不变）。

---

## 一、可行性评估

**结论：可行，但前提是把"科学的算命"重新定义为"可复现、可溯源的算命"。**

### 1.1 三个关键判断

**判断一："科学"不能指"预测准确"，只能指"过程严谨"。**

科学意味着可证伪、可重复。没有任何占卜方法能通过这个检验，宣称"科学算命"
既名不副实，也有广告法风险（见 §10 合规）。但有两个性质是可以真正做到的：

- **可复现**：同样的输入必然得到同样的结果。易经起卦公开算法与种子；
  星盘排盘由生辰唯一确定，用户拿任何一个通行排盘工具都能交叉核对。
- **可溯源**：结果中的每一句论断都能逐字对应到《周易》《紫微斗数全书》
  等公版典籍的原文，附书名、篇章、引文，用户可以翻书核对。

这两点合起来是本产品的差异化卖点。宣传口径用"有典可依、可考据、可复现"，
不用"科学"二字。

**判断二："不模棱两可"恰好是这两类典籍的长处，但要放对位置。**

传统算命"准"的心理机制是巴纳姆效应——话越模糊越容易对号入座。给出明确
结论意味着放弃这个保护伞：明确 + 说错 = 用户不满。

但所选典籍的文本结构恰好支持明确结论：

- 《周易》卦爻辞自带**断辞**——吉、凶、悔、吝、厉、无咎、亨、利。
  "潜龙勿用"就是明确说"不要行动"。
- 《紫微斗数全书》的诸星入宫论断与格局断语同样是明文判断——星曜有
  **庙旺平陷**的定量强弱，格局有"日月并明，佐九重于尧殿"式的明确断语。

因此明确性的正确落点是：

- ✅ 明确的**判断与建议**（此事：凶，不宜进／命盘：迁移宫强，利外出发展）
- ❌ 明确的**事件预言**（"你下个月会升职"）——必错无疑，且涉合规风险

敢于如实输出"凶、不宜"反而是最大差异化：不是我们说的，是典籍原文说的，
权威和责任都落在典籍上。

**判断三：用户满意度来自可解释性 + 仪式感，不依赖命中率。**

引用原文带来权威感；公开计算过程带来信任感；明确结论带来"得到了答案"的
满足感。三者都不依赖预测应验，产品逻辑成立。

### 1.2 双引擎的可行性

星盘引擎与易经引擎**同构**：排盘是确定性计算（甚至比起卦更确定——无随机
成分，生辰唯一决定命盘），断语查表自典籍，LLM 只解释。流水线的引文校验、
结构化输出、合规层全部复用。新增成本集中在两处：

1. **排盘引擎**（安星规则的代码化与测试）；
2. **第二典籍库**（数据工程量约为易经库的十倍，见 §5.3）。

双引擎在产品结构上也互补：命盘是**一次生成、长期复用的用户档案**（留存
与账户价值），起卦是**每问一事的高频互动**。"档案 + 高频"是健康的产品
结构。

### 1.3 主要风险

| 风险 | 说明 | 缓解 |
|------|------|------|
| 明确结论被证伪 | 用户记住错的预测 | 结论落在"建议"而非"预言"；忠于原文断辞，责任归典籍 |
| 双引擎结论互相矛盾 | "卦说凶、盘说吉"摧毁"不模棱两可"人设 | 结论单源原则 + 引擎分工（§6），禁止 LLM 折中综合 |
| LLM 幻觉引文 | 编造不存在的"古籍原文" | 引文逐字校验闸门（§7.4），不匹配即拒绝输出 |
| 平台审核 | 微信小程序禁占卜类目；App Store 中国区审核严格 | 以"国学文化 / 传统典籍学习"定位上架（§10） |
| 内容红线 | 医疗、投资、重大决策的确定性断言 | 问题分类器前置拒答 + 免责声明 |
| 生辰属个人敏感信息 | 排盘必须收集出生日期时辰 | 加密存储、明示用途、可删除（§10.5） |

---

## 二、核心设计原则

整个系统只有一条主线原则，其余都是它的推论：

> **LLM 不算命，只解释。所有"算"（起卦、排盘）是确定性代码；所有"引"
> 逐字校验；所有"结论"来自典籍明文的确定性映射；LLM 只负责讲清楚。**

五条推论：

1. **计算与解释分离**——起卦、变爻、选文、排盘、定庙陷全部是纯函数，
   单元测试全覆盖。这是"可复现"的来源。
2. **典籍是唯一事实来源**——LLM 的解读只能基于系统喂给它的原文片段，
   每个论断强制附出处。这是"可溯源"的来源。
3. **规则选文，杜绝挑拣**——读哪条爻辞由朱子占法决定，引哪条星曜断语
   由落宫与庙陷决定，全部写死在代码里。LLM 无法挑好听的说。
4. **结论单源**——任何一次回答，吉凶结论（verdict）只能来自一个引擎的
   断辞映射。两个引擎的判断**永远不做加权、折中、综合**（§6.3）。
5. **流派选择显式化**——占法、安星口诀存在流派差异（如早晚子时、闰月
   归属），选定一派后在结果中如实标注（"依朱熹《易学启蒙》占法""子时
   归属依××派"）。诚实标注本身是"严谨"人设的一部分。

---

## 三、总体架构

```
                 用户输入
      （所问之事 / 生辰 / 起卦参数）
                    │
                    ▼
        ┌───────────────────────┐
        │ ① 问事解析 + 路由器    │  问题分类；红线拒答；
        │    （LLM 轻量 + 规则） │  决定走哪个引擎（§6.1）
        └───────┬───────┬───────┘
        问事    │       │    问命/问运
                ▼       ▼
   ┌────────────────┐ ┌────────────────┐
   │ ② 事引擎：易经  │ │ ③ 命引擎：星盘  │
   │  起卦（确定性） │ │  排盘（确定性） │
   │  朱子占法选文   │ │  庙陷/格局定性  │
   │  断辞→结论映射  │ │  断语→结论映射  │
   └───────┬────────┘ └───────┬────────┘
           │    选定经文ID +   │
           │    确定性 verdict │
           ▼                   ▼
        ┌───────────────────────┐
        │ ④ 典籍知识库（双库）   │  结构化查表，非向量检索
        │  周易库 │ 紫微库       │
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ ⑤ 解读生成层（LLM）    │  只解释，不改判，逐句带出处；
        │                       │  合参时另一引擎仅作语境（§6.2）
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ ⑥ 引文校验器（确定性） │  逐字匹配双库；verdict 一致性
        └───────────┬───────────┘
                    ▼
   结果呈现（卦象/命盘图 + 原文 + 白话 + 明确结论 + 出处 + 复现凭证）
```

### 3.1 模块划分：数据 / 算法 / 前端

整个项目按三个模块组织。上图流程属于算法模块；数据模块在其下供
文本，前端模块在其上管呈现：

**1. 数据模块**——抓取、保存、检索文化典籍。
- 抓取：`tools/import_*.py`，一书一脚本、一次性运行（同系诸书可共
  一脚本，如六爻纳甲三典）。每部书记录
  底本页面与修订号（oldid）、许可、繁简转换方式、人工修字清单，
  重跑可逐字复现；导入即做完整性断言（章节数、关键语句在位），
  串节错位当场报错。底本原文与注疏混排、机器无法可靠剥离时宁缺
  勿滥（如序卦、杂卦弃《周易正义》页面而取白文《易傳》页面）。
- 保存：`tianwen/data/*.json`，进 git 受版本管理。统一形状：
  `meta`（来源、许可、校对状态）+ 单元列表/字典；每单元一个
  引文编号（cite_id，如 `xici:shang:9`、`meihua:1:li:xilinsi`、
  `ziwei:2:ming:ziwei`），是全项目引用文本的唯一坐标。
  校对流程与勘误记录见 `data/PROOFREADING.md`。
- 检索：`corpus.py` 统一门面——`catalog()` 藏书目录、`get(cite_id)`
  取文、`search()` 标点无关关键词检索；命令行
  `python -m tianwen.corpus`。语料不足 1MB，内存查表加线性
  扫描毫秒可回，故不用数据库、不用向量检索；将来语料增长时在
  corpus 门面之下换 SQLite FTS 等实现，接口不变。
- 现任务：让典籍更全面（十翼已齐；候选见 §9 路线图），检索结构
  已定于 cite_id 一层。

**2. 算法模块**——用户输入到输出的全流程（上图 ①–⑥）：
`topic.py`/`redline.py`（判类拦截）→ `casting.py`/`lunar.py`/
`strokes.py` 与 `ziwei/chart.py`（起卦排盘）→ `selection.py` 与
`ziwei/selection.py`（依占法选文）→ `verdict.py`（定例断辞）→
`llm.py`（解读，只解释不改判）→ `validator.py`（逐字引文校验）。
流程每一步须能从库中典籍找到依据并在凭证中给出 cite_id；
新占法（如大衍筮法）先入数据（系辞已在库），再入算法。

**3. 前端模块**——呈现，保持简单：`cli.py`（命令行）、`gui.py`
（本地图形界面）、`report.py`（文本排版）、`web/`（GitHub Pages
静态试用页：Pyodide 在浏览器内原样跑本包与校验闸门，无框架无后端；
API Key 用户自填存本机，请求直达 OpenRouter）。算法与数据两层
不为任何前端改形。

---

## 四、事引擎：易经

### 4.1 起卦（确定性代码）

支持四种起卦方式，各有明确的可复现机制：

**方式一：梅花易数·时间起卦（v3.5 起降为「当下之势」语境）**

完全确定性，无随机数。规则出自《梅花易数》卷一：

- 上卦 =（农历年支数 + 月数 + 日数）mod 8
- 下卦 =（农历年支数 + 月数 + 日数 + 时辰数）mod 8
- 动爻 =（年 + 月 + 日 + 时）mod 6
- （余数为 0 者取 8 / 6）

同一时辰问同一事，任何人复算结果一致。公历→农历转换库须锁定版本并对
边界日期（闰月、岁首）做测试。同一时辰**人人同卦、问什么都同卦**——
故 v3.5 起事类主断改由问语卦任之（方式四），时间卦恒起、只作当下之势
之参（问语无汉字可数时仍代主断并声明）。

**方式二：铜钱法·六爻（金钱卦）——v3.3 起撤出流程，代码留存**

规则出自《卜筮正宗》：三枚铜钱掷六次，三背为老阳(9)、三字为老阴(6)、
一背二字为少阳(7)、二背一字为少阴(8)，老阳老阴为动爻。

随机性处理曾为 `seed = SHA-256(问题文本 + 用户ID + 请求时间戳)`，seed 与
算法一并展示可复现六掷。但书上明说掷真钱，伪随机代掷非古籍明说
（ALGORITHM.md 总则 3），故撤出单一模式；待能接收用户真实掷象输入时
启用（朱子占法选文 select_zhuzi 一并留存）。

注意：铜钱法四种爻的概率为 1/8、1/8、3/8、3/8，与大衍筮法的 1/16、
3/16、5/16、7/16 不同。实现哪种就如实标注哪种。

**方式三：梅花易数·字占（以字画起卦，个人化起卦法）**

时间起卦同一时辰人人同卦；古法对"起卦须及于其人"的解法不是把生辰塞进
时间公式，而是字占——以其人所书之字起卦（《梅花易数》卷一「为人占」：
"或以书写来意……则以其字占之"）。规则出自卷一「一字占至十一字占」：

- 二字为两仪平分：一字为上卦，一字为下卦；
- 三字为三才：一字为上卦，二字为下卦；
- 各取总笔画 mod 8 配先天八卦数，总画数 mod 6 取动爻（余 0 取 8 / 6）。

占例即卷一「西林寺牌额占」（原文自注"系字画占例"）："以西字七画为艮，
作上卦；以林八画为坤，作下卦。以上七画下八画总十五画，除二六一十二，
余数得三，是山地剥卦。"

流派约定（凭证内如实标注）：

| 决策点 | 备选 | 本产品选定 |
|--------|------|------------|
| 笔画表 | 古籍计画 / 现代通行画数 | **Unihan kTotalStrokes**（Unicode 官方，版本随数据文件标注；古籍计画偶异——西林寺占记「西」七画，今作六画——凭证逐字列明画数以便核对） |
| 动爻加时 | 「爻以六除」章"取爻当以时加之" / 西林寺占例不加时 | **不加时**（从占例；起卦纯由所占之字确定，与时刻无关） |
| 一字 | 「以左为阳画，右为阴画」分卦 | **不受理**（须辨字形左右，无字形数据不妄断；提示改用两三字如姓名） |
| 四至十字 | 「不必数画数，只以平仄声音调之」（古四声） | **不受理**（入声之辨需韵书，暂未收录；如实拒之，不以今四声代古） |

**方式四：梅花易数·问语起卦（事类主断，v3.5）**

书写来意，以其字占之（卷一「为人占」：「或以书写来意……则以其字占之」）。
所问不同则卦不同，时辰由动爻入卦——同一时刻问不同的事，吉凶不再千人
一面（起卦及于所问之事，与字占及于其人同义）：

- 问语只数汉字（标点、字母、数字非字不入数）；
- 二三字：依字画起（两仪平分／三才，同姓名卦法）；
- 四字以上「字数分之」：少一半为上卦、多一半为下卦（均平对半，
  字占「如字数不匀，即少一字为上卦，取天轻清之义」；「十一字以上……
  止用字数。如字数均平，则以半为上卦，以半为下卦」），各以字数 mod 8
  配先天八卦；
- 动爻 =（总数＋时辰数）mod 6（「爻以六除」末句「取爻当以时加之」）。

流派约定（凭证内如实标注）：

| 决策点 | 备选 | 本产品选定 |
|--------|------|------------|
| 四至十字 | 「不必数画数，只以平仄声音调之」（古四声含入声） | **一律字数分之**（同「听其语声……即如其字数分之起卦」例；入声之辨需韵书，四声韵书缓收录 PROOFREADING，收后可启用平仄之法） |
| 动爻加时 | 「取爻当以时加之」通例 / 字画占例不加时 | **加时**（问语占的是时下之事，从通例；姓名卦仍不加时——所占者字，非时事） |
| 无汉字／仅一字 | —— | **回落时间卦代主断并声明**（一字须辨字形阴阳画，不妄断） |

粒度如实说明：四字以上卦体只由字数定，同字数之问同一时辰仍同卦体
（唯动爻同异随时）；欲完全随问而异，须俟真实掷象输入（v4 三期）。

**输出数据结构：**

```json
{
  "method": "meihua_time | meihua_zi | meihua_wenyu | coin",
  "reproducibility": { "seed": "…", "lunar_date": "…", "algorithm": "…" },
  "ben_gua":  { "id": 1, "name": "乾", "lines": [7,7,7,9,7,7] },
  "moving_lines": [4],
  "zhi_gua":  { "id": 9, "name": "小畜" },
  "hu_gua":   { "id": 1, "name": "乾" }
}
```

### 4.2 断卦规则（确定性代码）

两个职责，都是查表，都可单元测试：

1. **选文**：按变爻数量套用朱熹《易学启蒙》占法（附录A），输出应读的
   经文 ID。结果页标注"依朱熹《易学启蒙》占法"。
2. **定性**：从选定经文的断辞经附录B映射得出结论，如
   `{"verdict": "凶", "action": "不宜进，宜守"}`。LLM 只能解释，不能修改。

---

## 五、命引擎：星盘

### 5.1 星盘选型——推荐紫微斗数

"星盘"有三种可选实现，差异很大，必须先定：

| | **紫微斗数（推荐）** | 七政四余 | 西方占星 |
|---|---|---|---|
| 典籍依据 | 《紫微斗数全书》（题陈抟撰，明刊本，公版） | 《果老星宗》《星学大成》（公版） | 托勒密《占星四书》等（公版但非中文典籍） |
| 星的性质 | 虚星（口诀推演，不涉天文） | 实星（真实行星位置） | 实星（真实行星位置） |
| 计算确定性 | 完全确定（生辰→盘，纯查表口诀） | 确定（需天文历算/星历表） | 确定（Swiss Ephemeris） |
| 与国学定位契合 | ★★★ 完全契合 | ★★★ 契合但极冷门 | ✗ 与"古代典籍/国学"叙事冲突 |
| 断语文本结构 | 诸星入宫断语 + 庙旺平陷 + 格局，明文可查 | 有但整理难度大 | 有但需译介，出处叙事弱 |
| 用户认知度 | 高（"命盘"即紫微盘） | 极低 | 高但用户群不同 |
| 数据工程量 | 大（约为易经库十倍） | 极大 | 大 |

**结论：v2 用紫微斗数实现"星盘"。** 它就是中文语境里的"命盘"，生辰
唯一确定、全程无随机数，比易经起卦更"可复现"；断语出自公版典籍且自带
强弱定量（庙旺平陷），与"有典可依、不模棱两可"完全对齐；同时保住
"国学/传统文化"的上架定位。七政四余留作远期彩蛋（它用真实天文位置，
"可复现"故事最硬，但数据工程不现实）；西方占星除非另立产品线否则不做
——它会同时削弱典籍叙事和合规叙事。

排盘引擎接口设计为**可插拔**（`ChartEngine` 抽象），未来换/加星系不动
流水线。

### 5.2 排盘引擎（确定性代码）

输入：公历出生日期 + 时辰（+ 可选出生地经度，用于真太阳时校正）+ 性别
（大限顺逆需要）。

安星全部是《紫微斗数全书》明文口诀，逐条代码化：

1. 公历→农历（与事引擎共用同一转换库）；
2. 定命宫、身宫（寅宫起正月，顺数至生月，再起子时逆数至生时）；
3. 定五行局（纳音）；
4. 安紫微星（五行局 + 生日），十四主星依紫微、天府两系布宫；
5. 安辅弼昌曲、禄存羊陀、火铃空劫等辅煞星；
6. 定四化（生年天干）；
7. 定诸星庙旺平陷（查表）；
8. 起大限、流年。

**流派决策须显式选定并标注**：早/晚子时归属、闰月归属、真太阳时是否
启用。每项在结果页可见（"本盘依××：……"）。

**测试策略**：与多个通行排盘工具对照跑一批已知命例（覆盖闰月、子时、
年干边界），全部一致方可上线；此对照脚本保留为回归测试。

输出数据结构：

```json
{
  "reproducibility": { "solar_birth": "…", "lunar_birth": "…", "conventions": ["晚子时归次日", "未用真太阳时"] },
  "ming_gong": "午",
  "wuxing_ju": "金四局",
  "palaces": [
    { "name": "命宫", "branch": "午",
      "stars": [ { "name": "紫微", "brightness": "庙", "sihua": null } ] }
  ],
  "patterns": ["紫府同宫"],
  "da_xian": [ … ]
}
```

### 5.3 第二典籍库：紫微库

Schema（每条断语一条记录）：

```json
{
  "id": "ziwei-wuxing-ming",
  "star": "紫微", "palace": "命宫", "brightness": "庙",
  "text": "〔《紫微斗数全书》原文〕",
  "source": "紫微斗数全书·卷二·诸星入命宫吉凶论",
  "verdict_hint": "吉",
  "topic": null
}
```

量级估算：14 主星 × 12 宫 + 辅佐煞化诸星入宫 + 百余格局 + 大限流年
论断 ≈ **数千条**，仍是封闭小语料，继续查表、不用向量检索。但录入校对
的工作量约为易经库十倍，是 v2 的主要成本，需要专门的数据工程流程
（底本选定 → 录入 → 双人校对 → 断语 verdict_hint 人工审定）。

版权同易经库：古籍本身公版，不得直接抓取现代标点整理本，基于公版
影印本自行录入或使用明确开放授权的电子文本。

### 5.4 命盘的确定性结论

与断辞映射同理，命盘结论也不由 LLM 发挥：

- **强弱**来自庙旺平陷表（明文）；
- **吉凶**来自诸星入宫断语的 verdict_hint（逐条人工审定）与格局判断
  （格局识别是纯规则：星曜组合 → 格局名 → 典籍断语）；
- 输出如 `{"palace": "迁移宫", "verdict": "强", "basis": ["天马入庙", …]}`。

命盘结论的语义是**"此人此域的秉性与强弱"**，不是"某事会发生"——这是
与事引擎的本质分工，见下节。

---

## 六、双引擎协同（本设计最关键的一节）

### 6.1 分工与路由：卦断事，盘论人

传统术数本有分工：**卜筮问事**（一事一占，答"此事吉凶"），**命理论命**
（生辰定盘，答"此人秉性、所长所短、时运节律"）。双引擎照此分工，路由器
按问题类型分发：

| 问题类型 | 示例 | 引擎 | verdict 来源 |
|----------|------|------|--------------|
| 问具体事 | "这个月跳槽好不好" | 事引擎 | 卦爻断辞 |
| 问秉性格局 | "我适合什么方向" | 命引擎 | 星曜断语/格局 |
| 问时运 | "我今年运势如何" | 命引擎（大限流年） | 大限流年断语 |
| 混合 | "我这种人今年跳槽行吗" | 拆成两个子问题，分栏各答 | 各自单源 |

### 6.2 合参：另一引擎只作语境，不出结论

问具体事时，若用户已有命盘档案，命盘可以进入解读层——但只作为
**"人的语境"**，不产生第二个吉凶：

> "卦得××，爻辞'××'，断曰：不宜进（《周易·×·×》）。又，君命盘迁移宫
> ××入庙（《紫微斗数全书·×》），素有外向开拓之性，故此'守'字于君尤难，
> 更须自警。"

实现上：命盘摘要作为带出处的 context 传给 LLM，prompt 明令其只能用于
解释 verdict 的落地方式，不得据此加强、削弱或反转 verdict。引文校验器
同时校验两库出处。

**已实现**（`ziwei/selection.py: select_context` + `llm.py` 合参通道）：
问具体事且附生辰时，取与所问事类相应之宫的诸星断语（事业→官禄、婚恋
→妻妾、出行→迁移……无对应宫则命宫主星）作合参语境；输出中以「合参
语境」栏单列（◇ 非主断），LLM payload 中单独成块并明示约束，引文与
卦爻经文混合过同一逐字校验闸门。本地版无档案体系，生辰随问随填。

### 6.3 冲突红线（不可妥协）

1. **禁止折中**：LLM 永远不得把两个引擎的判断加权成一个中间结论
   （"卦虽凶但盘吉，总体尚可"是全设计最大的败笔，一旦出现，"不模棱
   两可"人设即告破产）。
2. **物理分栏**：当两个引擎的输出同时展示（混合问题），界面上分栏、
   各带各的出处链与 verdict，栏头明示各自回答的是什么问题（"就此事而占"
   ／"就君之命局而论"）。
3. **校验器兜底**：verdict 字段逐字等于对应引擎规则输出，出现未经路由
   授权的第二 verdict 即拒绝输出。

### 6.4 产品结构红利

- **命盘档案**：注册后一次排盘，长期挂在账户上——留存锚点，也免去每次
  输入生辰；
- **高频起卦**：日常问事走事引擎；
- 合参解读让老用户（有档案者）得到明显更个性化的结果，形成"建档"的
  自然动机。

---

## 七、共用层

### 7.1 输入信息清单（字段最小化原则）

**每个输入字段都必须能指出它被哪部典籍的哪条规则使用；用不上的坚决不收。**
收而不用是"伪仪式感"，与"有典可依"的人设直接冲突，且徒增个人信息合规
成本（个人信息保护法的最小必要原则）。

v3.3 单一模式输入固定五项（ALGORITHM.md 二）：

| 字段 | 单一模式 | 用途依据 |
|------|----------|----------|
| 所问之事 | **必需** | 红线检查、问事分类、占章选取、问事分宫 |
| 姓名 | 收（可缺） | 姓名卦：字画起卦论问者之位（卷一「一字占至十一字占」、西林寺占例——字画占本是《梅花易数》明文，非另立姓名学；单字/四字以上依书不受理，如实说明） |
| 问卦时刻 | 自动取当前时刻 | 梅花易数以年月日时起数（用户不输入） |
| 出生日期 | 收（可缺） | 安命宫、定五行局（《紫微斗数全书》安星诀） |
| 出生时辰 | 收（可缺） | 命宫由生月+生时定；缺时辰无法排盘，本次无盘 |
| 性别 | 收（可缺） | 大限顺逆（阳男阴女顺行）、入男/女命诀 |
| 出生地 | 不收 | 仅启用真太阳时校正时需要经度 |
| 昵称 | 不收 | 姓名已收，不再另设称呼字段 |

缺项只减少参照（不起姓名卦/无盘，输出如实说明），不改变流程形状。

### 7.2 问事解析与红线拦截

- 分类：事业、学业、情感、人际、出行、居所、决策抉择、命格、时运、其他。
  判类三级（v3）：关键词规则先行（确定性）→ 规则未中且已配模型时占者
  判类（温度 0，键限类别表，失败回落「其他」，输出标注来源）→ 用户指定
  最优先（界面下拉/`--topic`）。定类而占本是古法（《梅花易数》十八占、
  六爻按类取用神、紫微十二宫）；问题原文不因分类而丢——仍逐字入
  问事分宫与占者提示词。
- 红线：医疗健康诊断、投资理财、法律纠纷、生死寿夭直接婉拒并说明原因，
  宁可错杀。

### 7.3 解读生成层（LLM）

- 模型：Claude API（claude-sonnet-5 起步，效果不足再上 claude-opus-5），
  结构化输出（tool use / JSON schema）强制以下结构：

```json
{
  "conclusion": "白话结论（一至两句直接回答所问；纯白话，不带 cite 标注——结论先行）",
  "judgment": "断语（占者之断，一至两句，句末标 [cite_id]——无据不断；异于定例须明据；所据须落在主断侧文本）",
  "reasons": "解释理由（引原文逐字并白话释义，段末标出处编号）",
  "advice": "具体行动建议",
  "quotes": [ { "text": "原文逐字引用", "cite_id": "出处编号" } ]
}
```

- System prompt 核心约束（v3 定位修订后）：只能使用输入给出的经文与断语；
  **断（judgment）由模型任占者之职**——定例断辞为对照基准，从之须明其
  所以然，异断须明据，无据不得异断；不得软化辞气（不许把"凶"说成
  "略有不顺"）、不得模棱两可；合参语境不得跨引擎折中（§6.2/6.3）；
  紧扣所问类别；语气如实、克制、不恐吓、不承诺。
- 风格：**说人话**——如老练占者当面说话，有画面、有比方，以卦象之象
  作喻，落到问者生活场景，忌公文腔与术语罗列。势之档次可言而须有着落
  （原文等第之语白话转述并标出处、以象引申言成色），无据之许（数额、
  时限、"富可敌国"式必然结果）不做（ALGORITHM.md 六）。

### 7.4 引文校验器（幻觉闸门）

生成后、展示前的最后一道确定性检查：

- 每条 `quote` 逐字（去标点后）在对应知识库精确匹配，cite_id 须在本次
  给定文本集合内；
- `judgment` 必须含至少一个 [cite_id] 标注（无据不断），所标出处须在
  集合内；正文中方括号标注的 cite_id 同查；
- 定例断辞不经模型之手：由规则引擎算出并直接展示，模型无从篡改；
  引擎间仍禁出第二 verdict（§6.3）；
- 任一条不过 → 丢弃本次生成，带失败原因重试（上限 3 次），仍失败则降级
  为"仅原文 + 定例断辞"的无占断模式。
- 通过后附**占断存证**：模型名、通过校验的尝试次数、输出 JSON 的
  SHA-256——有条件可复现的凭证（定位修订节）。

### 7.5 多轮对话（Agent 部分）

- 追问经文/星曜细节（"九四爻什么意思""天相是什么星"）——仍走知识库 +
  引文校验；
- 重占引导：同一事短期重复占问设冷却期（"初筮告，再三渎，渎则不告"，
  既合古义又防刷）；命盘无此问题（盘不随问而变）；
- 追问不产生新 verdict，只围绕已出结果解释。
- 会话状态 `{ 问题, 卦象/命盘, 选文, verdict, 历史 }`，Claude Agent SDK
  或简单会话循环即可，不需要多 agent 编排。

### 7.6 呈现层

- 起卦动画 / 命盘十二宫图（动爻、庙陷、四化高亮）；
- 结果页固定结构：原文（竖排样式可加分）→ 白话 → 解读 → **明确结论**
  （视觉最突出）→ 建议 → 出处列表；
- **"验证"按钮**：事引擎展示起卦输入、算法、seed；命引擎展示生辰、
  流派约定，并附"可用任意通行排盘工具核对"的说明——"可复现"卖点的
  落地界面；
- 免责声明常驻页脚。

---

## 八、技术选型建议

| 部分 | 选型 | 理由 |
|------|------|------|
| 后端 | Python + FastAPI | 两个引擎都是纯函数库，易测试；农历转换有成熟库 |
| 引擎接口 | `DivinationEngine` / `ChartEngine` 抽象 | 星系可插拔（§5.1） |
| 知识库 | SQLite（周易库 + 紫微库两张表族） | 总语料 < 5MB，查表足矣 |
| LLM | Claude API，结构化输出 | 强制 citation 字段 |
| 前端 | 小程序 / H5（视上架策略） | 见 §10 |
| 测试 | pytest：64卦×7变爻情形全覆盖；排盘对照通行工具的命例回归集 | 确定性部分必须 100% 覆盖 |

---

## 九、MVP 范围与路线图

**MVP（v1，建议 2–3 周内可验证）——只做事引擎：**

1. 梅花易数时间起卦（零随机数，最能体现"可复现"）；
2. 周易库只含经文 + 大小象传；
3. 完整闭环：问事 → 起卦 → 朱子占法选文 → 断辞结论 → LLM 解读 →
   引文校验 → 结果页（含验证按钮）；
4. 不做多轮追问、注疏层、账户体系。

**v1.5：** 铜钱法（已随 v1 实现）；分类断法（topic_notes，已实现：
`topic.py` 关键词规则，类别附解读落点指引，标注"非典籍原文"入 LLM
提示词）；多轮追问（已实现：同卦续问，不重新起卦，逐轮过引文校验）；
注疏层第一期（已实现：王弼《周易注》598 条，自维基文库《周易正義》
经文锚定导入，CC BY-SA 4.0，注疏只作解读语境不参与断辞；程颐《伊川
易传》因维基文库标点本残缺、朱熹《周易本义》因仅有无标点四库本，
均暂缓，见 data/PROOFREADING.md）。

**v2 —— 命引擎（星盘）：**

1. 紫微排盘引擎 + 命例回归测试集（已实现：`ziwei/chart.py` 安星诀逐条
   代码化，21 命例对照 iztro 回归，流派分歧处按《全书》底本实现并标注）；
2. 紫微库第一期：十四主星入十二宫断语 + 庙旺平陷（已实现：`data/ziwei.json`
   429 条，自维基文库《紫微斗數全書》卷一至卷三导入；庙陷表逐格转录为
   代码表；命宫主星论断/分宫格/入男女命入限诀/逐宫断语/问答/大限论）；
3. 命盘档案（账户体系随之引入，生辰加密存储；本地 CLI 版暂以命令行参数
   传生辰、不落盘存储，账户档案留待联网版）；
4. 路由器升级：问命/问秉性走命引擎（已实现：命格/时运类问题附生辰即走
   紫微命引擎，命格取命宫主星断语、时运取现行大限；缺生辰退回事引擎
   并提示；结论由庙陷表与《论大限十年祸福何如》明文确定性映射）。

**v2.5：** ~~格局识别（查表半）~~、~~大限流年~~、~~合参解读~~（§6.2）
均已实现。**格局识别之查表半**：《卷一》得地合格诀／失陷破格诀按安命
宫支机取入语境（诀文所言星曜须对照盘面，只作参照不改结论单源）；
**格局判定器**（定富贵贫贱诸局按星曜组合认局）已于 v3.6 实现（见下）。
**流年二限**：太岁（当年年支）与小限（《卷二·安小限诀》，起宫按本生
年支三合、男顺女逆逐年一宫）所在之宫如实列出，太岁冲大限小限、冲羊陀
七杀诸项依《卷三·论二限太岁吉凶》明文逐项查验，小限宫主星附入限诀
为参——结论仍单源，定例仍依《论大限》三例。另已实现**问事分宫**：
问运带具体题材（财运/事业运/桃花运…）时按关键词规则加取所涉之宫诸星
断语（《全书·卷二》十二宫），题材宫只入解读，结论仍单源。

**v3（定位修订，已实现）：** 断归占者——LLM 依所据原文下占断（judgment，
无据不断，校验器强制），定例断辞降为对照基准；占断存证（模型 +
SHA-256）落地"有条件可复现"。后续之确定性召回层（书桌）第一期已实现：
盘为主断时按主断宫主星星名自《卷一》赋文格诀池逐行机械召回候选断语，
召回规则与命中数进凭证，入语境可引不可断、模型池内择引；按宫/类别
召回已随 v3.6 扩（见下）。

**v3.1 典籍扩充第一期（已实现）：** 易传补编——《说卦传》十一章
（类象诸章按八卦再分）与乾坤《文言》（按所释经文单元锚定），自维基
文库《周易正義》导入（data/yizhuan.json）；《梅花易数》卷二体用总诀
＋十八占（data/meihua.json）。接线：梅花法算体用两卦（动爻所在为用）
并附说卦广象；文言挂乾坤经文单元随选文自动附入（经传原文可引为据）；
体用总诀于梅花法恒附，占章按问事类别附取（selection.TOPIC_ZHAN，
判类三级之类别至此有典籍占章呼应；类别未映射时按题材关键词回落，
ASPECT_ZHAN 复用紫微分宫表）。校验器新规：占断不得单据王弼注
或说卦取象立断。后续候选：说卦一至六章入系辞语境、《焦氏易林》
4096 繇辞、六爻纳甲全文（v4 级）。

**v3.1.5 数据模块自立（已实现）：** 按 §3.1 三模块划分立数据模块。
典籍扩充第二期——《梅花易数》卷一全卷入库（起卦诸法原文、观梅占
西林寺牌额占等占例、八卦类象、八卦万物属类按八卦切分，起卦引擎所
引各章至此逐条可检索可引用）；十翼补齐——《系辞上下传》（大衍筮法
所出）、《序卦传》、《杂卦传》自白文《易傳》页面导入（《周易正义》
系辞序卦杂卦页面韩注与传文裸行混排、机器无法可靠剥离，故弃之，
宁取白文）。统一检索层 `corpus.py`（catalog / get / search ＋命令行）。
后续候选：大衍筮法作第四种起卦方式（典据 xici:shang:9 已在库）；
《焦氏易林》4096 繇辞；六爻纳甲全文（v4 级）。体用取象接万物属类
逐卦单元（meihua:1:xiang:wanwu:*）已随 v3.6 实现（附于体用广象之下）。

**v3.2 卦盘并占（已实现，后为 v3.3 单一模式取代）：** 曾以 DualSession
实现"两问两断、并陈不合断"。v3.3 起交互收敛为单一模式，并占撤下；
"混池合断出一个吉凶"仍明确不做——单一模式以主断唯一＋语境合参达成
同一目的（吉凶单源，§6.3 不破），而非并陈两断。

**v3.3 单一模式（已实现，规范见 ALGORITHM.md）：** 输入固定五项
（问什么＋姓名＋生日＋出生时辰＋性别），时间卦、姓名卦、紫微盘三占
同起，全部确定性——非古籍明说不用随机数，铜钱法（伪随机代掷）撤出
流程、代码留存待真实掷象输入。主断路由（问事→时间卦，命理→紫微盘），
其余占法以语境块入解读层，校验器强制断语所据落在主断侧（主断唯一，
结构上杜绝自相矛盾）。输出结论先行：白话结论→断语→理由（引原文）→
建议→一览→凭证。解读 JSON 字段改为 conclusion / judgment / reasons /
advice / quotes。

**v3.4 典籍扩充第三期（已实现，数据侧）：** 紫微库二期——《全书》
卷一赋文与格局诸诀 91 条（太微赋、形性赋、骨髓赋、女命骨髓赋、十等论、
得地合格诀／失陷破格诀按安命宫支、定富贵贫贱杂四局按局名逐条），
v2.5 格局识别之典据自此在库（接线另议）；六爻纳甲典籍层——
《京氏易传》（八宫六十四卦逐卦本文，卦序 id 与周易库对齐）、
《火珠林》、《黄金策》三库入藏（v4 六爻起卦之典据先行，只入藏书
检索层，不参与断辞不入选文）。经评估缓收并录明缘由（详
data/PROOFREADING.md）：《焦氏易林》（标点本 4/64 残缺，四库本无标点
且行内连排）、《易学启蒙》（无单行本，仅通释四库混排本）、
《卜筮正宗》（十四卷全缺仅存序）、《增删卜易》（录入稿未校、今人
自撰内容混排）——底本不过关者宁缺，维持「逐字有据」。

**v3.5 问语入卦（已实现）：** 事类主断由时间卦改问语卦——书写来意，
以其字占之（卷一「为人占」）；二三字依字画、四字以上字数分之（平仄
古四声法无韵书缓用），动爻加时数（「取爻当以时加之」），同一时刻问
不同事不再同卦；时间卦恒起、降为「当下之势」语境（两种主断下皆然），
问语无汉字可数则回落时间卦代主断并声明。凭证全程逐字列算式；
网页/CLI/GUI 经 service 门面同步生效。详 §4.1 方式四与 ALGORITHM.md
步骤 3。

**v3.6 格局判定器（已实现）：** 《卷一》定富贵贫贱诸局按星曜组合认局
（ziwei/patterns.py）——诀文自述星曜宫位条件可机判者 23 局逐条转码为
判定式（每条注明原文；文义约定——夹＝邻宫、拱＝三合、向＝对宫、
「同守身命」＝分守二宫等——随局注明并录 ALGORITHM.md 步骤 8），问命格
时对照盘面认局，认出之局连同盘面依据入语境（已合盘面可引，不可断，
不改结论单源）；不可机判者 26 局宁缺、缘由进凭证（诀作「见前批注」
无判据 11 局、涉空亡 5 局〔截路旬中空亡诸星未安〕、涉天刑 1 局、
文义两可 1 局〔禄马佩印〕、定杂局论限运盛衰 8 局）。个别夹局依安星诀
星序实不可能成局（如财荫夹印之武梁夹相），照诀转码、不合则不出。
两处文义取舍以同列句例为据：「逢巨暗」承「生不逢时」之「逢廉贞」
解作巨门守命；「四杀守身命」承「财与囚仇」之「同守身命」解作分守
身命二宫——均因字面强读经安星诀推演结构性不可能而改从句例。
另（卦侧）：体用取象接《梅花易数》八卦万物属类逐卦单元
（meihua:1:xiang:wanwu:*，v3.1.5 所候），附于体用广象之下入解读。
另（书桌二期）：盘主断且问涉具体题材时，所涉之宫名并入书桌召回
（按宫；问事类别经分宫映射归于宫名），凭证以「所涉之宫某」标出。

**v3+：** 回访机制（"当时问的事后来如何"——留存手段 + 独有数据）；
远期可选七政四余。

**候选（详 ALGORITHM.md 七）：** 西洋占星本命盘——由生辰确定、无随机，
有公版典籍（如托勒密《占星四书》）可依，合"逐步有据"原则；待数据
模块收录公版底本后再议（塔罗等须洗牌之法，按"非明说不随机"暂缓）。

**明确不做：** 八字（与紫微功能定位重叠，命理择一深做）；任何付费
改运、开光、法物销售（合规红线）。

---

## 十、合规与伦理（务必重视）

1. **平台审核**：微信小程序类目不含且实际禁止"占卜"；App Store 中国区
   对迷信内容审核严格。上架定位为**国学/传统文化教育工具**，产品叙事为
   "《周易》《紫微斗数全书》典籍的检索、学习与传统占法演示"。本设计
   可溯源、标注占法出处、展示原文，实质上就是典籍应用工具，不是话术包装。
2. **宣传用语**：不使用"科学算命""预测准确"（广告法 + 名不副实）。用
   "有典可依、可考据、可复现"。
3. **红线内容**：医疗、投资、法律、寿夭类问题一律拒答（§7.2）。
4. **免责声明**：所有结果页注明"内容源自古代典籍原文及传统占法，属传统
   文化范畴，仅供参考，不构成任何现实决策依据"。
5. **生辰隐私（v2 新增）**：出生日期时辰（及出生地）属个人敏感信息——
   加密存储、单独告知并取得同意、仅用于排盘、支持一键删除档案；未成年人
   按平台要求处理。

---

## 十一、附录

### 附录A：朱子占法（《易学启蒙》）——变爻选文规则

| 变爻数 | 所读经文 |
|--------|----------|
| 0 | 本卦卦辞 |
| 1 | 本卦变爻爻辞 |
| 2 | 本卦两变爻爻辞，以上爻为主 |
| 3 | 本卦卦辞（贞）+ 之卦卦辞（悔）合参 |
| 4 | 之卦两不变爻爻辞，以下爻为主 |
| 5 | 之卦不变爻爻辞 |
| 6 | 乾坤两卦读用九/用六，余卦读之卦卦辞 |

（梅花易数起卦固定一个动爻：动爻所在之卦为用、不动之卦为体，动爻
爻辞主断，本卦、之卦卦辞为参，体用两卦取象（《说卦传》广象）与
体用总诀、所问类占章（《梅花易数》卷二）入解读——依《梅花易数》
体用之说。）

### 附录B：断辞 → 结论映射（示例，需随数据工程细化）

| 断辞 | verdict | action 基调 |
|------|---------|-------------|
| 元吉 / 大吉 | 吉 | 宜进，可大胆行动 |
| 吉 / 亨 / 利… | 吉 | 宜进 |
| 无咎 | 平 | 可行，行动本身无过失 |
| 悔 / 吝 | 谨 | 可行但有代价，宜慎 |
| 厉 | 危 | 有风险，非必要不动 |
| 凶 | 凶 | 不宜进，宜守静待时 |
| 无断辞 | 依象传定性 | 映射表穷举 384 爻逐条人工审定 |

注：一条爻辞可含多个断辞（如"贞吉，悔亡"），映射按整句人工审定，不做
自动拆词。

### 附录C：命引擎流派约定清单（实现前逐项选定并全站统一标注）

| 决策点 | 备选 | 本产品选定（结果页均如实标注） |
|--------|------|------------|
| 晚子时（23–24时）归属 | 当日 / 次日 | **当日**（不换日，时辰作子；与事引擎一致） |
| 闰月归属 | 归本月 / 归下月 / 月中分界 | **整月归下月**（《全书·安身命例》明文「闰正月生者要在二月内起安身命」） |
| 年干支分界 | 正月初一 / 立春 | **正月初一**（与事引擎一致） |
| 真太阳时 | 启用（需出生地）/ 不启用 | **不启用**（不收出生地） |
| 庙旺平陷等级表 | 各版本略有出入 | **维基文库《全书·卷二》庙陷表**逐格转录（如七杀在酉作旺，与流行工具偶有出入） |
| 壬干四化之科 | 天府（《全书》）/ 左辅（流行诀） | **天府**（依《全书》「壬梁紫府武」明文） |
| 辛干魁钺 | 魁寅钺午（《全书》「虎马」）/ 魁午钺寅（流行诀「马虎」） | **魁寅钺午**（依底本） |
| 大限首限 | 起命宫（通行）/《安大限诀》字面另读 | **起命宫**、起限之岁为局数（从通行，便于用户交叉核对） |
| 宫无正曜 | 借对宫 / 不借 | **借对宫**（通行借宫法，标注「借宫而论」） |
| 时区 | 系统本地时 / 北京时间 | **一律北京时间（东八区）**：农历由东八区定义。起卦/论限「当前时刻」自动由系统时钟换算为北京时间（海外运行与国内同刻同卦，保可复现）；生辰按出生地钟表时间填写（中国大陆即北京时间，海外出生先自行换算），程序不作换算（双引擎一致） |

---

## 十二、一句话总结

**把"算"做成可复现的确定性代码（起卦与排盘），把"据"做成逐字可校验的
双典籍查表，把 LLM 限制在"解释者"的位置上，把"明确结论"锚定在古人明文
写下的断辞与断语上，并用"卦断事、盘论人"的分工保证双引擎永不互相和稀泥
——这样"有依据、不模棱两可"的承诺就不依赖模型的自觉，而是系统结构保证
的。**
